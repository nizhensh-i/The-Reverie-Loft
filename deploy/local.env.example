# 本地部署配置（local 模式）
# 复制为 deploy/local.env 后按需修改

# 本地 backend 目录（可选）
LOCAL_BACKEND_DIR=./backend

# local 默认使用 development compose
COMPOSE_FILE_BASE=docker-compose.dev.yaml

# 应用镜像
BACKEND_IMAGE=nizhenshi/flasky_backend
BACKEND_TAG=latest

# 基础设施镜像（可选）
MYSQL_IMAGE=mysql/mysql-server:8.0
REDIS_IMAGE=redis:latest

# 服务名（与 compose 文件对应，可选）
BACKEND_SERVICE_NAME=backend
MYSQL_SERVICE_NAME=mysql
REDIS_SERVICE_NAME=myredis
